name: 更新年度进度条
on:
  schedule:
    - cron: '0 16 * * *'  # 每天北京时间0点运行
  workflow_dispatch:     # 手动触发

permissions:
  contents: write  # 显式授权写入
  pull-requests: write

jobs:
  update-progress:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取仓库代码
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0  # 拉取完整历史，避免分支问题

      - name: 设置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 打印当前目录和文件（排查路径）
        run: |
          pwd
          ls -la  # 确认README.md存在
          cat README.md | grep "年度进度条"  # 确认标记存在

      - name: 计算并更新年度进度
        run: |
          # 写入脚本（修复标记被覆盖问题，核心修改）
          cat > calculate_progress.py << 'EOF'
          import datetime
          import re

          today = datetime.date.today()
          year = today.year
          total_days = 366 if (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0) else 365
          days_passed = (today - datetime.date(year, 1, 1)).days + 1
          progress_percent = round((days_passed / total_days) * 100, 2)
          date_str = today.strftime("%d-%b-%Y")

          # 打印计算结果（方便排查）
          print(f"计算的进度：{progress_percent}%")
          print(f"计算的日期：{date_str}")

          # 读取README（指定编码，避免乱码）
          with open("README.md", "r", encoding="utf-8") as f:
              content = f.read()

          # ========== 核心修改1：兼容新旧标记，优先用包裹式标记 ==========
          # 第一步：检查是否已有包裹式标记（第一次运行时可能没有，先初始化）
          has_progress_wrap = '<!--PROGRESS_START-->' in content and '<!--PROGRESS_END-->' in content
          has_date_wrap = '<!--DATE_START-->' in content and '<!--DATE_END-->' in content

          if not has_progress_wrap or not has_date_wrap:
              # 初始化包裹式标记（替换原来的一次性标记）
              content = re.sub(
                  r'<!--YEAR_PROGRESS_PERCENT-->',
                  f'<!--PROGRESS_START-->{progress_percent}%<!--PROGRESS_END-->',
                  content
              )
              content = re.sub(
                  r'<!--YEAR_PROGRESS_DATE-->',
                  f'<!--DATE_START-->{date_str}<!--DATE_END-->',
                  content
              )
              print("初始化包裹式标记完成")
          else:
              # 已有包裹式标记，只替换中间的数值（保留标记）
              content = re.sub(
                  r'<!--PROGRESS_START-->.*?<!--PROGRESS_END-->',
                  f'<!--PROGRESS_START-->{progress_percent}%<!--PROGRESS_END-->',
                  content
              )
              content = re.sub(
                  r'<!--DATE_START-->.*?<!--DATE_END-->',
                  f'<!--DATE_START-->{date_str}<!--DATE_END-->',
                  content
              )
              print("更新包裹式标记内的数值完成")

          # ========== 核心修改2：兼容检查，避免脚本退出 ==========
          # 兜底检查：确保至少有一个标记存在
          if not has_progress_wrap and '<!--YEAR_PROGRESS_PERCENT-->' not in content:
              print("警告：未找到进度相关标记，请检查README中的进度条行格式！")
              # 不退出，仅警告，避免脚本中断

          # 写入文件
          with open("README.md", "w", encoding="utf-8") as f:
              f.write(content)

          # 验证替换结果
          with open("README.md", "r", encoding="utf-8") as f:
              new_content = f.read()
          print("替换后的进度条行：")
          for line in new_content.split("\n"):
              if "年度进度条" in line:
                  print(line)
          EOF

          # 执行脚本并打印日志
          python calculate_progress.py

      - name: 配置Git并推送
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git

          # 打印Git状态（排查是否有修改）
          git status

          git add README.md
          # 提交并推送（强制提交，避免空判断问题）
          git commit -m "🤖 自动更新年度进度条: $(date +'%Y-%m-%d')" || echo "无修改无需提交"
          git push origin main -f  # 强制推送（仅用于测试，后续可去掉-f）
